---
- name: PSQL DDL - ADD USER SYSTEM
  community.postgresql.postgresql_query:
    login_host: "{{ ansible_host }}"
    login_port: "{{ psql.host.port }}"
    login_user: "{{ psql.db.user }}"
    login_password: "{{ psql.db.password }}"
    db: "{{ psql.db.name }}"
    query: "call sp_create_user_system_role(%s, %s);"
    positional_args:
      - "{{ system.username }}"
      - "{{ system.password }}"
  loop: "{{ psql.user.system }}"
  loop_control:
    loop_var: system